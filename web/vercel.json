{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "comment": "Proxy API calls to the Render backend (same-origin from the browser, no CORS). REPLACE the host with your Render URL.",
      "source": "/api/:path*",
      "destination": "https://lockdin-api.onrender.com/api/:path*"
    },
    {
      "comment": "SPA fallback — every non-file, non-API route serves index.html so client routing works.",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
