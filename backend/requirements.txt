# Pinned to the versions verified working in this project's environment
# (import + OpenRouter scoring + all endpoints exercised). python-jose was
# removed — it is not installed and nothing imports it (auth is verified via the
# Supabase client, not python-jose).
fastapi==0.109.2
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
httpx==0.28.1
supabase==2.28.2
pydantic==2.12.5
pydantic-settings==2.2.1
apscheduler==3.11.2
jinja2==3.1.6
